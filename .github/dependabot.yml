# Actions are pinned to commit SHAs (a tag can be moved to point at other code). Dependabot
# keeps those pins current and updates the trailing "# v4" comment with the new version.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore"
  # The hash-pinned locks in requirements/ are what CI and contributors actually install, so
  # security alerts and updates must cover them, not only the ranges in pyproject.toml. For a
  # full refresh (every pin, re-resolved together), run `python scripts/lock_deps.py --upgrade`.
  - package-ecosystem: pip
    # Only /requirements: that is where the pinned, hash-locked files live. Listing "/" as
    # well produced a second, identical pull request for every bump - pyproject.toml carries
    # ranges, and the locks are what an install actually resolves to.
    directory: /requirements
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore"
    ignore:
      # A lock is a whole resolved graph, and Dependabot bumps one pin at a time. filelock 4
      # is the case that proved it: virtualenv pins filelock<4, so the bump made every
      # `pip install --require-hashes` in CI fail with ResolutionImpossible. It will move on
      # its own the next time `python scripts/lock_deps.py --upgrade` re-resolves everything.
      - dependency-name: filelock
        versions: ["4.x"]
